<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>Confirm Password Change</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f5f8fa;
        padding: 20px;
        color: #333;
      }
      .container {
        max-width: 600px;
        margin: auto;
        background: #ffffff;
        border-radius: 8px;
        padding: 30px;
        box-shadow: 0 0 10px rgba(0,0,0,0.05);
      }
      .header {
        font-size: 22px;
        font-weight: bold;
        color: #2c3e50;
        margin-bottom: 20px;
      }
      .content {
        font-size: 16px;
        line-height: 1.5;
      }
      .otp-box {
        background-color: #eef3f7;
        padding: 15px;
        text-align: center;
        font-size: 24px;
        letter-spacing: 4px;
        margin: 20px 0;
        border-radius: 6px;
        font-weight: bold;
        color: #007bff;
      }
      .footer {
        margin-top: 40px;
        font-size: 12px;
        color: #999;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">Confirm Your Password Change</div>
      <div class="content">
        <p>Hi {{firstName}},</p>
        <p>
          We received a request to change the password for your account. Please use the verification code below to confirm this action:
        </p>
        <div class="otp-box">{{otpCode}}</div>
        <p>
          This code is valid for {{otpExpiry}} minutes. If you did not request this change, please ignore this email or contact our support team.
        </p>
        <p>Thanks,<br>{{companyName}}</p>
      </div>
      <div class="footer">
        Need help? Contact us at support@example.com
      </div>
    </div>
  </body>
</html>
